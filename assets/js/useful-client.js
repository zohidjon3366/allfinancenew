(function(){var cards=Array.prototype.slice.call(document.querySelectorAll('.useful-card[href^="#useful-"]'));var panels=Array.prototype.slice.call(document.querySelectorAll('.useful-static-panel'));function setActive(){var hash=location.hash||(panels[0]?('#'+panels[0].id):'');cards.forEach(function(card){card.classList.toggle('active',card.getAttribute('href')===hash);});}cards.forEach(function(card){card.addEventListener('click',function(){setTimeout(setActive,30);});});window.addEventListener('hashchange',setActive);setActive();})();